{
  "name": "lightningdjs",
  "version": "2.0.0",
  "description": "LightningDJS handles slash-commands/events for DiscordJS V14 Bots.",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rmdir /s /q dist && tsc",
    "build": "yarn clean && tsc",
    "dev": "cd test && ts-node index.ts",
    "start": "yarn run build && yarn run dev"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/LuxTheFox/LightningDJS.git"
  },
  "author": "Lux",
  "license": "GPL-3.0",
  "files": ["dist", "LICENSE"],
  "dependencies": {
    "discord.js": "^14.6.0"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "@types/node": "^18.11.9",
    "prettier": "^2.7.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}
